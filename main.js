const name = prompt("Введите ваше имя");
document.body.appendChild(NewTag)
const NewTag = document.name("h1")


































































































